区块链原理与技术项目作业
该项目的启动方式如下：

（1）链端：src/webase-deploy
         运行指令：python deploy.py startNode
（2）后端：src/server
         使用eclipse开启项目运行
（3）前端: src/front-end
         使用`npm install`安装依赖后使用`npm run dev`开启项目，默认运行在127.0.0.1：8080

